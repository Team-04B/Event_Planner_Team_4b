import HeroSection from "@/components/homeComponents/heroSection/HeroSection";

const HomePage = () => {
    return (
        <section>
          <HeroSection/>
        </section>
    );
};

export default HomePage;